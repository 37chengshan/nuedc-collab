# 地猛星②：接收与显示端

通过 UART1 RX 中断接收地猛星①的数据帧，CRC 校验通过后立即回传 ACK，并在本地 OLED 显示与发送端一致的地址、链路、ADC、电压和序号。64 字节环形缓冲可避免 OLED 阻塞刷新期间丢失串口数据。

- UART1：PA8 TX、PA9 RX，115200 8N1
- OLED：PB2 SCL、PB3 SDA、地址 0x3C
- 接线：①PA8→②PA9，①PA9←②PA8，GND共地

后续透明无线串口模块保持 115200 8N1，即可替代三根有线连接。

# 前端看板归档

前端工作已于 2026-07-29 按用户要求暂停。源码没有删除，只从活动工作区移出，避免默认安装、构建和测试继续依赖页面。

## 原路径映射

- `apps/dashboard/` → `归档/前端看板/apps/dashboard/`
- `tests/e2e/` → `归档/前端看板/tests/e2e/`
- 根 `playwright.config.ts` → `归档/前端看板/playwright.config.ts`
- 根前端检查脚本 → `归档/前端看板/scripts/`
- 原根配置快照 → `归档/前端看板/原根配置/`

## 以后恢复

1. 新建单独分支。
2. 将上述路径按映射移回。
3. 参考 `原根配置/package.json` 恢复前端 workspace 与脚本。
4. 重新安装依赖，运行单元测试和 E2E。
5. 恢复前先确认文件通讯协议和安全 Git API 没有发生破坏性变化。

当前不要从此目录运行生产服务，也不要修改活动根 `package.json` 来自动构建前端。
